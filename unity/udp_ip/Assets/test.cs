using UnityEngine;
using System.Collections;
using System;
public class test : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log(1000 + Convert.ToInt32(gameObject.name) );
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
